# Netlify ОТКЛЮЧЁН — продакшен на VPS (см. server/VPS-UPDATE.txt).
# Если сайт ещё привязан к Netlify: Deploys → Stop builds, или удалите site.
# Локальная сборка publish-* по-прежнему: npm run build:cpe|fce|ege

[build]
  ignore = "exit 0"
