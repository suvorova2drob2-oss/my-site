[build]
  command = "node scripts/build-netlify-track.mjs cpe"
  publish = "publish-cpe"

[build.environment]
  NODE_VERSION = "20"

# Per-track outputs: publish-cpe | publish-ege | publish-fce (see scripts/NETLIFY-SPLIT.txt)
# Redirects are generated inside each publish-* folder as _redirects.
