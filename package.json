{
  "name": "my-site-live-quiz",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "vite",
    "vite:build": "vite build",
    "build": "npm run vite:build && node scripts/build-netlify-track.mjs cpe",
    "build:cpe": "npm run vite:build && node scripts/build-netlify-track.mjs cpe",
    "build:ege": "npm run vite:build && node scripts/build-netlify-track.mjs ege",
    "build:fce": "npm run vite:build && node scripts/build-netlify-track.mjs fce"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "typescript": "~5.7.3",
    "vite": "^6.2.0"
  }
}
