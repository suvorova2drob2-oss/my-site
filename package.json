{
  "name": "my-site-live-quiz",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check:tracks": "node scripts/check-tracks.mjs",
    "start": "node server.js",
    "dev": "vite",
    "vite:build": "vite build --mode cpe",
    "vite:build:ege": "vite build --mode ege",
    "vite:build:fce": "vite build --mode fce",
    "build": "npm run vite:build && node scripts/build-netlify-track.mjs cpe",
    "build:cpe": "npm run vite:build && node scripts/build-netlify-track.mjs cpe",
    "build:ege": "npm run vite:build:ege && node scripts/build-netlify-track.mjs ege",
    "build:fce": "npm run vite:build:fce && node scripts/build-netlify-track.mjs fce"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "typescript": "~5.7.3",
    "vite": "^6.2.0"
  }
}
